{
  "name": "branch-guard",
  "version": "1.0.0",
  "description": "Prevent merge conflicts by blocking dangerous branch switches",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "branch-guard": "dist/cli.js"
  },
  "scripts": {
    "build": "ncc build src/cli.js -o dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "git",
    "branch",
    "merge-conflict",
    "workflow",
    "cli"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "ora": "^8.0.1",
    "simple-git": "^3.22.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
